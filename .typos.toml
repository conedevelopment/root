[files]
extend-exclude = [
    ".git/",
    "composer.lock",
    "yarn.lock",
    "public/build/assets/",
    "public/build/manifest.json",
]
ignore-hidden = false

[default.extend-words]
"encrypter" = "encrypter"
"invokable" = "invokable"
"responsable" = "responsable"

[default.extend-identifiers]
"conedevel" = ""
